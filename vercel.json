{
  "buildCommand": "cd e-library && npm install && npm run build",
  "outputDirectory": "e-library/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
